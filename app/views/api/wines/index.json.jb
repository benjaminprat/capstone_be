 @wines.map do |wine|
  {
    id: wine.id,
    style: wine.style,
    producer_wine: wine.producer_wine,
    grape: wine.grape,
    region: wine.region,
    country: wine.country,
    vintage: wine.vintage,
    
  }
  end